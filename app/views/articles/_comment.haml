.comment{class: (reply.zero? ? "" : "reply#{reply}")}
  = comment.text
  %hr
  .commentdetails
    %ul
      %li
        = image_tag "http://data.matchvote.com/images/2015/highprofile/Joe_Biden.png",
          class: "img", title: "Joe Biden"
      %li
        %strong Joe Biden
      %li from DE
      %li
        = comment.created_at.strftime("%m/%d/%y")
        @
        = comment.created_at.strftime("%l:%M%p")
      %li &nbsp
      %li
        .btn.btn-xs.btn-default
          Report
        .btn.btn-xs.btn-default
          Hide Replies
        .btn.btn-xs.btn-default
          32
          .glyphicon.glyphicon-heart
        .btn.btn-xs.btn-black
          Close Reply
    .writereply
      .row
        .col-md-10
          .form-group
            %textarea.form-control
              :preserve
                Reply to Joe Biden
        .col-md-2
          %center
            .btn.btn-sm.btn-primary
              Submit
- comment.comments.each do |comment_reply|
  = render "comment", comment: comment_reply, reply: reply + 1
