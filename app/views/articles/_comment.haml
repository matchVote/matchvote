.comment{class: comment.reply_level_class(reply),
         style: comment.displayed?(reply),
         data: { id: comment.id, reply_ids: comment.reply_ids }}
  = comment.text
  %hr
  .commentdetails
    %ul
      %li
        = image_tag "http://data.matchvote.com/images/2015/highprofile/Joe_Biden.png",
          class: "img", title: "Joe Biden"
      %li
        %strong Joe Biden
      %li from DE
      %li
        = comment.created_at_date
        @
        = comment.created_at_time
      %li &nbsp
      %li
        .btn.btn-xs.btn-default
          Report
        - if comment.has_replies?
          .btn.btn-xs.btn-default.show-replies
            Show Replies
          .btn.btn-xs.btn-default.hide-replies{style: "display:none"}
            Hide Replies
        .btn.btn-xs.btn-default
          32
          .glyphicon.glyphicon-heart
        .btn.btn-xs.btn-black
          Close Reply
    .writereply
      .row
        .col-md-10
          .form-group
            %textarea.form-control
              :preserve
                Reply to Joe Biden
        .col-md-2
          %center
            .btn.btn-sm.btn-primary
              Submit
- comment.comments.each do |comment_reply|
  = render "comment",
    comment: CommentPresenter.new(comment_reply),
    reply: reply + 1
