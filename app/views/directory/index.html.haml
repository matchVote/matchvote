.row
  .filter_menu
    .col-md-8
      %h5
        435 elected officials meet your search criteria.
        Add more filters to refine your search.
      %button.btn.btn-danger
        Add Filter
    .col-md-4
      %br
      %strong
        = select_tag "Sort", options_for_select(@sort_list),
          class: "form-control",
          id: "rep_sorter"

.row
  %div
    - @reps.each do |rep|
      .directory_block
        = image_tag "http://matchvote.com/wp-content/uploads/2015/01/obama.png",
          class: "directory_pic"
        %h4= rep.full_name
        %div= "#{Random.rand(100)}% match"

        %br
        = link_to rep_path(rep.profile_id) do
          %button.btn.btn-sm.btn-default View Profile
        %button.btn.btn-sm.btn-default Follow
