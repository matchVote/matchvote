.container
  .col-md-8
    = render "breadcrumbs", rep: @rep

    .whitecard
      = image_tag @rep.profile_image_url,
        class: "profile_pic"
      = render "edit_button", rep: @rep if policy(@rep).edit?
      %div{data: { role: "biography_container" }}
        .profile_name
          .follow-button-container
            - if current_user.following?(@rep)
              = render "relationships/unfollow_button", followed: @rep
            - else
              = render "relationships/follow_button", followed: @rep
          %h2= @rep.full_name
          %hr
          #{@rep.government_role} | #{@rep.status}
          %br
          #{@rep.party} from #{@rep.state}
        .profile_bio
          %p#short_bio= truncate(@rep.biography, length: 550)
          %p#full_bio{style: "display:none;"}= @rep.biography
          = button_tag "Read Full Bio",
            class: "btn btn-sm btn-default bio_btn",
            data: { behavior: "toggle_bio" }


    .row
      .col-md-8
        %h2 Recent News
      .col-md-4
        %br
        .btn.btn-sm.btn-link
          Find more news about #{@rep.full_name}
    .whitecard
      %h4
        ‘Fox & Friends’ Books Entirely Wrong Guest, Gets Anti-Trump Rant on the Air
      %hr
      .div.pull-right
        .btn.btn-xs.btn-default
          Comments
        .btn.btn-xs.btn-default.share-article
          .glyphicon.glyphicon-share{title: "Share"}
          Share
        .btn.btn-xs.btn-default.bookmark
          .glyphicon.glyphicon-bookmark{title: "Bookmark Article"}
          Bookmark
      %span By Andrew Kirell on July 23, 2018

    .whitecard
      %h4
        It’s Time for a Mass Movement to Impeach Trump
      %hr
      .div.pull-right
        .btn.btn-xs.btn-default
          Comments
        .btn.btn-xs.btn-default.share-article
          .glyphicon.glyphicon-share{title: "Share"}
          Share
        .btn.btn-xs.btn-default.bookmark
          .glyphicon.glyphicon-bookmark{title: "Bookmark Article"}
          Bookmark
      %span By Miles Mogulescu on July 23, 2018

    .whitecard
      %h4
        Donald Trump is reportedly fuming over North Korea’s failure to honor his handshake
      %hr
      .div.pull-right
        .btn.btn-xs.btn-default
          Comments
        .btn.btn-xs.btn-default.share-article
          .glyphicon.glyphicon-share{title: "Share"}
          Share
        .btn.btn-xs.btn-default.bookmark
          .glyphicon.glyphicon-bookmark{title: "Bookmark Article"}
          Bookmark
      %span By Adam Peck on July 22, 2018

  .col-md-4
    -# .well.first_widget
    -#   %h3
    -#     = "#{Random.rand(100)}%"
    -#     Approval Rating
    -#   %div
    -#     %i Based on #{@rep.citizen_ratings_total} citizen ratings
    -#   %br
    -#   %a.btn.btn-danger
    -#     Rate #{@rep.nickname_or_first_name}
    -#   %a.btn.btn-default
    -#     Read Comments

    -# .profile_tweet
      %span Tweet

    .well.first_widget
      %h3
        Connect with #{@rep.nickname_or_first_name}
      %strong #{@rep.title} #{@rep.nickname_or_first_name} #{@rep.last_name}
      %br
      - @rep.contact.postal_addresses.each do |address|
        = address.line1
        %br
        = address.city_state_zip
      %br
      - @rep.contact.phone_numbers.each do |number|
        %span.glyphicon.glyphicon-phone-alt
        = number
      - if @rep.contact.contact_url
        = button_tag id: "contact_button", class: "btn btn-link" do
          %span.glyphicon.glyphicon-send
          = link_to "Email", @rep.contact.contact_url, target: "_blank"
      %hr
      = link_to "Facebook", @rep.facebook_url, target: "_blank",
        class: "btn btn-default btn-sm"
      = link_to "Twitter", @rep.twitter_url, target: "_blank",
        class: "btn btn-default btn-sm"
      = link_to "YouTube", @rep.youtube_url, target: "_blank",
        class: "btn btn-default btn-sm"

    .well
      %center
        %h4 About #{@rep.full_name}
      %br
      %table.table.table-hover
        %tr
          %td.col-md-5 Gender
          %td= @rep.gender
        %tr
          %td Age
          %td= @rep.age
        %tr
          %td Birthday
          %td= @rep.birthday_formatted
        %tr
          %td Religion
          %td= @rep.religion
        %tr
          %td Orientation
          %td= @rep.orientation

    -# .well.widget= render "issue_comparison", issues: @issues
