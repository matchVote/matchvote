.row
  .stances_menu
    .col-md-6
      %h3 Where do you stand on the following issues?
      %p 
        Save at least 15 stances to be matched with elected officials in the 
        Directory. The more stances you save, the more accurately you will be 
        matched.
    .col-md-6
      %center
        %h3 You've saved 21 of 42 possible stances.
        .progress
          .progress-bar{aria: { valuemans: "42", valuemin: "0", valuenow: "21" }, 
            role: "progressbar", style: "width: 50%;"}

.row.stances-index
  #stances_index
    .col-md-2
    .col-md-9
      - @categories.each do |issue|
        .row
          %h2= issue.name.titleize
          - issue.statements.each do |statement|
            .statement{data: { statement_id: statement.id }}
              = render "statement",
                statement: statement,
                stance: statement.find_stance_for_user(current_user) || Stance.new
    .col-md-1
